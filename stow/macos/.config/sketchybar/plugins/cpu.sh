#!/usr/bin/env bash

# Omarchy parity: fixed icon, no color shifts, no labels — minimal.
# Click opens activity monitor (sketchybarrc click_script).
sketchybar --set "$NAME" icon="󰍛" icon.color=0xffa9b1d6 label.drawing=off
