[user]
	name = Sebastian Sogamoso
	email = sebastian@sogamo.so
	signingkey = 
[github]
  user = sebastiansogamoso
[credential]
  helper = osxkeychain
[core]
  editor = vim
  excludesfile=~/.gitignore
[color]
  ui = always
[alias]
  a         = add
  co        = checkout
  pom       = pull origin main
  prom      = pull --rebase origin main
  fop       = fetch origin --prune
  amend     = commit -C HEAD --amend
  amendm    = commit --amend
  today     = log --since=midnight --author='Sebastian Sogamoso' --oneline
  last-week = log --since='1 week ago' --no-merges --oneline
  remote-diff = diff main...origin/main --oneline
[hub]
  protocol = https
